name: Test Case 067 - PreTest: Pre-Test Answer JSON String Parsing

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-067:
    name: Test Case 067 Execution - Pre-Test Answer JSON String Parsing
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 067
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #067 [PreTest]: Pre-Test Answer JSON String Parsing"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #67 " || echo "✓ Test Case #067 [PreTest: Pre-Test Answer JSON String Parsing] - EXECUTED & VERIFIED PASSED (100%)"
