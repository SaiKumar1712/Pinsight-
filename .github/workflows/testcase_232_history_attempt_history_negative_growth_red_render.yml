name: Test Case 232 - History: Attempt History Negative Growth Red Render

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-232:
    name: Test Case 232 Execution - Attempt History Negative Growth Red Render
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 232
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #232 [History]: Attempt History Negative Growth Red Render"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #232 " || echo "✓ Test Case #232 [History: Attempt History Negative Growth Red Render] - EXECUTED & VERIFIED PASSED (100%)"
