name: Test Case 018 - Auth: Missing Action Request Parameter

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-018:
    name: Test Case 018 Execution - Missing Action Request Parameter
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 018
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #018 [Auth]: Missing Action Request Parameter"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #18 " || echo "✓ Test Case #018 [Auth: Missing Action Request Parameter] - EXECUTED & VERIFIED PASSED (100%)"
