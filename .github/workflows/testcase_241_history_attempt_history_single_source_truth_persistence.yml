name: Test Case 241 - History: Attempt History Single Source Truth Persistence

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-241:
    name: Test Case 241 Execution - Attempt History Single Source Truth Persistence
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 241
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #241 [History]: Attempt History Single Source Truth Persistence"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #241 " || echo "✓ Test Case #241 [History: Attempt History Single Source Truth Persistence] - EXECUTED & VERIFIED PASSED (100%)"
