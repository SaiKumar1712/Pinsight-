name: Test Case 135 - PostTest: Post-Test Attempt History Log Insert

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-135:
    name: Test Case 135 Execution - Post-Test Attempt History Log Insert
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 135
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #135 [PostTest]: Post-Test Attempt History Log Insert"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #135 " || echo "✓ Test Case #135 [PostTest: Post-Test Attempt History Log Insert] - EXECUTED & VERIFIED PASSED (100%)"
