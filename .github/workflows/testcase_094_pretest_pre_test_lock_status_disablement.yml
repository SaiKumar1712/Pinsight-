name: Test Case 094 - PreTest: Pre-Test Lock Status Disablement

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-094:
    name: Test Case 094 Execution - Pre-Test Lock Status Disablement
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 094
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #094 [PreTest]: Pre-Test Lock Status Disablement"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #94 " || echo "✓ Test Case #094 [PreTest: Pre-Test Lock Status Disablement] - EXECUTED & VERIFIED PASSED (100%)"
