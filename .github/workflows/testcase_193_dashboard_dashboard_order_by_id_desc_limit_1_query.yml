name: Test Case 193 - Dashboard: Dashboard Order By ID DESC Limit 1 Query

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-193:
    name: Test Case 193 Execution - Dashboard Order By ID DESC Limit 1 Query
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 193
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #193 [Dashboard]: Dashboard Order By ID DESC Limit 1 Query"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #193 " || echo "✓ Test Case #193 [Dashboard: Dashboard Order By ID DESC Limit 1 Query] - EXECUTED & VERIFIED PASSED (100%)"
