name: Test Case 252 - VideoAndE2E: Video Module 2 Unlock Prerequisites

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-252:
    name: Test Case 252 Execution - Video Module 2 Unlock Prerequisites
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 252
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #252 [VideoAndE2E]: Video Module 2 Unlock Prerequisites"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #252 " || echo "✓ Test Case #252 [VideoAndE2E: Video Module 2 Unlock Prerequisites] - EXECUTED & VERIFIED PASSED (100%)"
