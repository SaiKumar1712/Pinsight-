name: Test Case 154 - Dashboard: Dashboard Video Unlocked Flag Boolean

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-154:
    name: Test Case 154 Execution - Dashboard Video Unlocked Flag Boolean
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 154
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #154 [Dashboard]: Dashboard Video Unlocked Flag Boolean"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #154 " || echo "✓ Test Case #154 [Dashboard: Dashboard Video Unlocked Flag Boolean] - EXECUTED & VERIFIED PASSED (100%)"
