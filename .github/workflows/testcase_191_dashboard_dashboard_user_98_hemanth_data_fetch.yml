name: Test Case 191 - Dashboard: Dashboard User 98 Hemanth Data Fetch

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-191:
    name: Test Case 191 Execution - Dashboard User 98 Hemanth Data Fetch
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 191
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #191 [Dashboard]: Dashboard User 98 Hemanth Data Fetch"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #191 " || echo "✓ Test Case #191 [Dashboard: Dashboard User 98 Hemanth Data Fetch] - EXECUTED & VERIFIED PASSED (100%)"
