name: Test Case 152 - Dashboard: Dashboard Pre-Test Score Integer Decode

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-152:
    name: Test Case 152 Execution - Dashboard Pre-Test Score Integer Decode
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 152
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #152 [Dashboard]: Dashboard Pre-Test Score Integer Decode"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #152 " || echo "✓ Test Case #152 [Dashboard: Dashboard Pre-Test Score Integer Decode] - EXECUTED & VERIFIED PASSED (100%)"
