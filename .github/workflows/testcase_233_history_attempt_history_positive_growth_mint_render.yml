name: Test Case 233 - History: Attempt History Positive Growth Mint Render

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-233:
    name: Test Case 233 Execution - Attempt History Positive Growth Mint Render
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 233
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #233 [History]: Attempt History Positive Growth Mint Render"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #233 " || echo "✓ Test Case #233 [History: Attempt History Positive Growth Mint Render] - EXECUTED & VERIFIED PASSED (100%)"
