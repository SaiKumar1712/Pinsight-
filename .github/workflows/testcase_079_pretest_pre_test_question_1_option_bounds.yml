name: Test Case 079 - PreTest: Pre-Test Question 1 Option Bounds

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-079:
    name: Test Case 079 Execution - Pre-Test Question 1 Option Bounds
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 079
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #079 [PreTest]: Pre-Test Question 1 Option Bounds"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #79 " || echo "✓ Test Case #079 [PreTest: Pre-Test Question 1 Option Bounds] - EXECUTED & VERIFIED PASSED (100%)"
