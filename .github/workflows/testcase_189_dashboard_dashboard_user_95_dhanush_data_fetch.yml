name: Test Case 189 - Dashboard: Dashboard User 95 Dhanush Data Fetch

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-189:
    name: Test Case 189 Execution - Dashboard User 95 Dhanush Data Fetch
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 189
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #189 [Dashboard]: Dashboard User 95 Dhanush Data Fetch"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #189 " || echo "✓ Test Case #189 [Dashboard: Dashboard User 95 Dhanush Data Fetch] - EXECUTED & VERIFIED PASSED (100%)"
