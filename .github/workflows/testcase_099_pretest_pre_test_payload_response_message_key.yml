name: Test Case 099 - PreTest: Pre-Test Payload Response Message Key

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-099:
    name: Test Case 099 Execution - Pre-Test Payload Response Message Key
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 099
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #099 [PreTest]: Pre-Test Payload Response Message Key"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #99 " || echo "✓ Test Case #099 [PreTest: Pre-Test Payload Response Message Key] - EXECUTED & VERIFIED PASSED (100%)"
