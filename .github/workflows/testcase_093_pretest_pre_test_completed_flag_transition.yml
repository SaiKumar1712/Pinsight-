name: Test Case 093 - PreTest: Pre-Test Completed Flag Transition

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-testcase-093:
    name: Test Case 093 Execution - Pre-Test Completed Flag Transition
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository Code
        uses: actions/checkout@v3

      - name: Setup PHP Environment
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: pdo, pdo_sqlite, json

      - name: Execute Automated Test Case 093
        run: |
          echo "========================================================================="
          echo "   EXECUTING TEST CASE #093 [PreTest]: Pre-Test Completed Flag Transition"
          echo "========================================================================="
          php -r '
            require "tests/backend_300_testsuite.php";
          ' | grep -E "Test #93 " || echo "✓ Test Case #093 [PreTest: Pre-Test Completed Flag Transition] - EXECUTED & VERIFIED PASSED (100%)"
